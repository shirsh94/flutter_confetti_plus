name: flutter_confetti_plus
description: A Flutter confetti package with overlays, controller widgets, emoji particles, cannon blasts, celebration presets, timelines, and motion modes.

version: 0.0.3

homepage: https://github.com/shirsh94/flutter_confetti_plus
repository: https://github.com/shirsh94/flutter_confetti_plus
issue_tracker: https://github.com/shirsh94/flutter_confetti_plus/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  vector_math: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
